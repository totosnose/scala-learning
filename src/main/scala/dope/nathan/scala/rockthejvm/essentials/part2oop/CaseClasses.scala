package dope.nathan.scala.rockthejvm.essentials.part2oop

object CaseClasses {
  // 1. class params are fields
  // 2. sensible toString
  // 3. equals and hashCode implemented OOTB
  // 4. CCs have handy copy method
  // 5. CCs have companion object
  // 6. CCs are serializable
  // 7. CCs have extractor patterns (pattern matching)

  case object Uk // excludes p. 5
}
