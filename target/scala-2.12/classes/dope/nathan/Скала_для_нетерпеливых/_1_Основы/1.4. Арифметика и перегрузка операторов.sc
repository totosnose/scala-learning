// операторы + - * / % и & | ^ >> <<
val answer = 8 * 5 + 2

// операторы являются методами
// a method b "==" a.method(b)
val a, b = 2
a + b
a.+(b)

1 to 10
1.to(10)

// нет ++ и --
var counter = 0
counter += 1

// метод /% - частное и остаток от деления в BigInt
// BigInt и BigDecimal с обычными операторами
val x: BigInt = 1234567890
x * x * x

// существует перегрузка операторов